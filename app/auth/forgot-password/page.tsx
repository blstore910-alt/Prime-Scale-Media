import { ForgotPasswordForm } from "@/components/forgot-password-form";

// Straight into the auth shell's .side column; the form is the card.
export default function Page() {
  return <ForgotPasswordForm />;
}
